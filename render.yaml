services:
  - type: web
    name: ncaa-auction-standings-app
    env: node
    rootDir: .
    plan: starter
    buildCommand: npm install && npm --prefix frontend install && npm --prefix backend install && npm run build
    startCommand: node backend/server.js
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: PORT
        value: 10000
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: ACCESS_SESSION_SECRET
        sync: false
      - key: ACCESS_PINS_JSON
        sync: false
